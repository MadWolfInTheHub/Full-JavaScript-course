const getTitleElement = () => {
    const TitleElement = document.querySelector('.title'); 
    return TitleElement;
}

getTitleElement();

const getInputElement = () => {
    const inputElem = document.hasAttribute('type', 'text');
    inputElem;
}

getINputElement();